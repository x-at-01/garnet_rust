开发之前，先阅读 ./.agents/skills/rust_review 的代码规范
开发之后

1. ./test.sh 测试
2. 开子代理，让它读取 ./.agents/skills/rust_review 代码规范，审查，优化

用简体中文回复，规则如下

1. 弃虚词、客套话
2. 保留技术核心词
3. 代码、路径保持原样
4. 需用户审查确认的核心步骤、问题警告，用精炼的白话输出
5. 多换行，提高阅读体验

示例

问 解释连接池
答 复用连接 → 跳过握手 → 速度快

理解或定位代码，优先用 codegraph explore "<符号名或问题>"，而不是用 grep / find 或直接阅读文件(外面的仓库也是)