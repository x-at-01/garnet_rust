把../garnet （C#存储过程部分除外）改写为 rust

一个一个对 crate 开子代理，code review，确保 rust 代码正确、完整、高效、简洁、优雅地实现了 C#的代码和相关测试

主代理负责调度，告知子代理对标哪些 c#文件审查代码
子代理开 worktree 到/tmp/fork/下面，写完、测试之后合并到当前文件夹，清理 worktree。如果遇到主分支修改，请提交，然后合并。

子代理审查，优化，代码规范见 .agents/skills/rust_review/SKILL.md

对于同一个模块，反复开子代理审查代码（循环），直到子代理汇报已经正确、完备、高效、简洁、优雅地的实现的相关的 C# 代码和测试

如果有问题，修订后，要检查上下游调用，确保逻辑和 c# 吻合（我们自己的优化除外），在注释中标注和 c#的文件、函数的对应关系，实现的异同

清理 hack 式编程，主体逻辑尽量参考 c#

注意，我们对 c# 进行了改造优化

gossip 用的是我们自定义的明文 quic

hset、zset、set 的编码我有优化，不是完全的 1:1 复刻 c#

acl 不用配置文件，记录到了数据库

acl 支持千万用户，用户支持名字空间，每个空间可以独立的 select u64 个 db，db 都相互隔离

要效验我们优化逻辑的正确性、空间复杂度、时间复杂度

键要是二进制安全的，相互隔离

无需兼容 c#，清理为兼容 c# 写的代码

凡事只需要一种实现，删除为兼容 c#写的代码

./test.sh 测试遇到问题， 请先梳理 c#是怎么做的，然后对照修改

重复代码请拆分复用，模块设计低耦合、高内聚，参考 c#

用 rust 的方式编程，为性能优化编码

序列化用 bitcode ，hash 用 gxhash
配置文件用 https://crates.io/crates/nested_text 格式

compio 是一个线程一个 cpu，利用这一点优化

禁用 #[allow(dead_code)]，删除无用的代码

子代理结束之后，用中文汇报一下，并修订 TODO.md （禁止使用加粗）