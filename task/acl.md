查看 acl 和相关 redis 命令的实现

请不断开子代理，执行下面的流程，直到子代理返回正确、简洁、优雅、高效的实现了相关命令

acl 可以给用户绑定名字空间

我要求，绑定了名字空间的用户，创建的用户，自动绑定就是当前名字空间，并且也只能绑定当前名字空间（否则返回错误）

请 code review，是否正确、高效、简洁、优雅

acl 命令，在集群中的分发实现是否正确？

有问题，让子代理开 worktree 到/tmp/fork/下面，优化，写完、测试之后合并到当前目录，清理 worktree。如果遇到主分支修改，请提交，然后合并。

不同名字空间的 select db 要相互隔离，其 flushall，只清理用户自己的名字空间；其 flushdb 也只能清理自己的数据库（确保有测试，验证隔离，但是不要写重复的测试，清理冗余的测试）

清理速度要快，后台慢慢 gc